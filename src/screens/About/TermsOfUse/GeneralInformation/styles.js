import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  header: {
    fontWeight: 'bold',
    fontSize: 20,
    marginTop: 25,
  },
  p: {
    fontSize: 16,
  },
  title: {
    fontWeight: 'bold',
    fontSize: 16,
  },
});
